# Ignore Yarn lock file
package-lock.json

# Ignore the "node_modules" directory
node_modules

# Ignore the "dist" directory (output of your build process)
dist

# Ignore the ".cache" directory (e.g., cache folders created by tools)
.cache